{
  "name": "dern-sample-app",
  "packageManager": "yarn@3.6.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "npm-run-all 'compile:*'",
    "compile:graphql": "yarn workspace @sample/graphql-server compile"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.1.6"
  }
}
